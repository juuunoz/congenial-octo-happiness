tooling decisions: \
use simple react app without typescript, faster and no future plans to scale up. scaleable design more important. \
use react framework also for dev speed, and reactive states for rendering. \
need reactive rendering because i want to process prices list in chunks, and update rendering per chunks \
design decisions: \
ignore missing column handling for now \
display multiple etfs at once -> need to refactor into table component. didnt do \